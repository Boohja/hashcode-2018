# hash.out
## Installation
checkout repo and run
```
npm install
```

## Development
Use `nodemon` for hot-reload
```
npm i -g nodemon
```
No need to install it globally, but hey it's cool.

Run with
```
nodemon
```